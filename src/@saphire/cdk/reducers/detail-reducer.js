const initialState = {
  isFetching: false,
  error: null,
  data: null
}

const DetailReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default DetailReducer
